{% extends 'base.html.twig' %}

{% form_theme newUserForm 'bootstrap_4_layout.html.twig' %}

{% block title %}
    Nouvel Utilisateur
{% endblock %}

{% block main %}
    <style>
        label, h1{
            color:#272353;
        }
    </style>
    <div class="navbar navbar-expand-lg col-12 shadow-sm" style="background: #fffffe">
        <a href="{{ path('homePanel') }}">
            <button class="btn btn-outline-primary mx-3"><i class="fal fa-arrow-to-left"></i> Retour</button>
        </a>
        <h5>Nouvel utilisateur</h5>
    </div>
    <div class="container">
    <div class="row justify-content-center">
        <div class="col-12 col-sm- 10 col-md-8 col-lg-6 p-3 mt-5">
            <h1 class="text-center">Nouvel utilisateur </h1>
        </div>
    </div>
    <div class="row justify-content-center">
        <div class="col-12 col-sm-10 col-md-8 col-lg-6  p-3">
            {% block form %}
                {{ form_start(newUserForm) }}
                {{ form_widget(newUserForm) }}
                <br>
                <button type="submit" class="btn btn-outline-primary btn-block">Ajouter l'utilisateur
                </button>
                {{ form_end(newUserForm) }}
            {% endblock %}
        </div>
    </div>
    </div>
{% endblock %}



